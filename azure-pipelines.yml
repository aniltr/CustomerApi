# Deploy to Azure Kubernetes Service
# Build and push image to Azure Container Registry; Deploy to Azure Kubernetes Service
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master

variables:

  # Container registry service connection established during pipeline creation
  dockerRegistryServiceConnection: '228ad6ab-3e0c-4d64-9b13-d331653d2a06'
  imageRepository: 'customerapi'
  containerRegistry: 'acrcustomerapi.azurecr.io'
  dockerfilePath: '**/Dockerfile'
  tag: '$(Build.BuildNumber)'
  
  # Agent VM image name
  vmImageName: 'ubuntu-latest'
  

stages:
- stage: Build
  displayName: Build stage
  jobs:  
  - job: Build
    displayName: Build
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        command: buildAndPush
        repository: $(imageRepository)
        dockerfile: $(dockerfilePath)
        containerRegistry: $(dockerRegistryServiceConnection)
        tags: |
          $(tag)
          
    - publish: manifests
      artifact: manifests

- stage: Deploy
  displayName: Deploy stage
  dependsOn: Build

  jobs:
  - deployment: Deploy
    displayName: Deploy
    pool:
      vmImage: $(vmImageName)
    environment: 'aniltrCustomerApi.default'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: KubernetesManifest@0
            displayName: Deploy to Kubernetes cluster
            inputs:
              action: deploy
              manifests: |
                $(Pipeline.Workspace)/manifests/customer-data-secret.yml
                $(Pipeline.Workspace)/manifests/customer-persistent.yml
                $(Pipeline.Workspace)/manifests/customer-data-deployment.yml
                $(Pipeline.Workspace)/manifests/customer-data-service.yml
                $(Pipeline.Workspace)/manifests/customerapi-deployment.yml
                $(Pipeline.Workspace)/manifests/customerapi-service.yml
              containers: |
                $(containerRegistry)/$(imageRepository):$(tag)

